node HelloKarma {

    struct Point {
        x: float,
        y: float
    }
    
    fn main() -> () {
        var p: Point = {
            x: -10.5,
            y: 5.0,
        };

        print_float(p.x);
        print_char(' ');
        print_float(p.y);
        println();
    }

}